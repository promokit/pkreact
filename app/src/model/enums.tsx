export enum SidebarStates {
  Open = 1,
  Close = 0
}

export enum NotificationType {
  Error = 'error',
  Info = 'info',
  Warning = 'warning'
}

export enum AddToCartAction {
  Up = 'up',
  Down = 'down'
}
